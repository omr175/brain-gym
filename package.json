{
  "name": "brain-gym",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "lint": "node --check app.js && node --check src/logic.mjs && node --check scripts/build.mjs && node --check scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm run lint && npm run test && npm run build"
  }
}
